SIPhone - cross-platform SIP softphone(for windows/linux)

Сперва будет разрабатываться версия под Windows, потом уже под Linux. 

Данная программа будет разрабатыватся на основе библиотеки pjsip (http://pjsip.org).

Под Windows проект разрабатывается в Visual Studio 2010, однако в нём не компилируются библиотеки pjsip`а, поэтому для компиляции проекта нужно будет подключить уже скомпилированную библиотеку( например, скомпилированную в VS 2008).